# Data

Runtime and training data belongs here locally, but it is intentionally not committed.

Common local paths are:
- `raw/` for source project text and Minecraft reference material
- `processed/` for cleaned training data
- `tokenizer/` for tokenizer artifacts

The app creates directories as needed.
